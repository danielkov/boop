name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check for release
    runs-on: ubuntu-latest
    outputs:
      released: ${{ steps.apply.outputs.released }}
      version: ${{ steps.meta.outputs.version }}
      changelog: ${{ steps.meta.outputs.changelog }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build boop
        run: cargo build --release

      - name: Apply pending changes
        id: apply
        run: |
          if ./target/release/boop apply; then
            echo "released=true" >> "$GITHUB_OUTPUT"
          else
            echo "No pending changes, skipping release."
            echo "released=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Get version and changelog
        if: steps.apply.outputs.released == 'true'
        id: meta
        run: |
          VERSION=$(./target/release/boop version)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

          EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
          echo "changelog<<$EOF" >> "$GITHUB_OUTPUT"
          ./target/release/boop changelog >> "$GITHUB_OUTPUT"
          echo "$EOF" >> "$GITHUB_OUTPUT"

      - name: Update crate version
        if: steps.apply.outputs.released == 'true'
        run: |
          sed -i 's/^version = ".*"/version = "${{ steps.meta.outputs.version }}"/' Cargo.toml
          cargo update --workspace

      - name: Commit and push
        if: steps.apply.outputs.released == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .boop/releases.toml Cargo.toml Cargo.lock
          git commit -m "$(cat <<'MSG'
          ci(boop): release ${{ steps.meta.outputs.version }} [skip ci]

          ${{ steps.meta.outputs.changelog }}
          MSG
          )"
          git tag "v${{ steps.meta.outputs.version }}"
          git push origin main --tags

  build:
    name: Build ${{ matrix.target }}
    needs: check
    if: needs.check.outputs.released == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            archive: tar.gz
            cross: true
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: v${{ needs.check.outputs.version }}

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: matrix.cross
        uses: taiki-e/setup-cross-toolchain-action@v1
        with:
          target: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          cd target/${{ matrix.target }}/release
          tar -czvf ../../../boop-${{ matrix.target }}.tar.gz boop
          cd ../../..

      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cd target/${{ matrix.target }}/release
          7z a ../../../boop-${{ matrix.target }}.zip boop.exe
          cd ../../..

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: boop-${{ matrix.target }}
          path: boop-${{ matrix.target }}.${{ matrix.archive }}

  release:
    name: Create Release
    needs: [check, build]
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" \) -exec mv {} release/ \;
          cd release && sha256sum * > checksums.sha256

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.check.outputs.version }}
          name: v${{ needs.check.outputs.version }}
          body: ${{ needs.check.outputs.changelog }}
          files: release/*
          draft: false
          prerelease: ${{ contains(needs.check.outputs.version, '-') }}
